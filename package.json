{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run generate-icons && nuxt build",
    "dev": "nuxt dev --host",
    "generate": "npm run generate-icons && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "generate-icons": "node scripts/generate-icons.js"
  },
  "dependencies": {
    "firebase": "^11.2.0",
    "nuxt": "^3.15.3",
    "nuxt-icon": "^0.5.0",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.13.1",
    "@vite-pwa/nuxt": "^0.10.6",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.1",
    "sharp": "^0.33.5",
    "tailwindcss": "^3.4.17"
  }
}
